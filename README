FTP client for Google Go language

example:

func main() {
    ftp := new(FTP)
     ftp.Connect("10.0.0.1", 20)
     ftp.Login("user", "****")
     ftp.Request("TYPE I")
     ftp.Stor("a.txt", "/path/a.txt")
     ftp.List()
     ftp.Pwd()
     ftp.List()
     ftp.Quit()
}
